{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:docker": "docker-compose up",
    "clean:docker": "docker-compose rm -f",
    "start:dev": "lerna run start:dev --stream"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9",
    "npm-run-all": "^4.1.5"
  }
}
